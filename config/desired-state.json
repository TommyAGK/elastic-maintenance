{
  "integrations": [
    { "name": "endpoint", "version": "latest" }
  ],
  "fleet_policies": [
    { "name": "default-endpoint-policy", "description": "Baseline endpoint policy" }
  ],
  "rules": [
    { "name": "Suspicious login", "type": "query", "enabled": true, "prebuilt": false }
  ]
}

